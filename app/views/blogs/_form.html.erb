<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
        <% blog.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <h4> <%= form.label :title %></h4>
    <%= form.text_field :title %><br><br>
  </div>

  <div class="field">
    <h4><%= form.label :body %></h4>
    <%= form.text_area :body, class: "form-control" %><br>
  </div>

  <div class="field">
    <h4><%= form.label :user_id %> </h4>
    <%= form.number_field :user_id %> <br><br>
  </div>
 
  <div class="field">
    <h4><%= form.label :category_id %></h4>
    <%= form.select :category_id, Category.all.map{|c|[c.category,c.id]} %><br>
  </div>
<br>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
